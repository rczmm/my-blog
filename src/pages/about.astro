---
import AetherLayout from '../layouts/AetherLayout.astro';

// 获取文章统计信息
const allPosts = await Astro.glob('./posts/*.md');
const totalPosts = allPosts.length;
const totalTags = new Set(allPosts.flatMap(post => post.frontmatter.tags || [])).size;

// 计算博客运行天数
const startDate = new Date('2023-01-01');
const today = new Date();
const daysDiff = Math.ceil((today.getTime() - startDate.getTime()) / (1000 * 60 * 60 * 24));

// 技能数据
const skills = [
  { name: 'Java', level: 90 },
  { name: 'TypeScript', level: 85 },
  { name: 'SpringBoot', level: 88 },
  { name: 'Vue.js', level: 82 },
  { name: 'Node.js', level: 80 },
  { name: 'CSS/Sass', level: 85 },
  { name: 'Astro', level: 75 },
  { name: 'Git', level: 88 }
];

// 时间线数据
const timeline = [
  {
    year: '2023',
    title: '系统初始化',
    description: 'Aether Lab 系统初始化，开始记录技术日志',
    icon: '🚀'
  },
  {
    year: '2022',
    title: '全栈开发',
    description: '扩展至后端领域，掌握 React 和 Node.js 核心技术',
    icon: '💼'
  },
  {
    year: '2021',
    title: '前端进阶',
    description: '深入研究现代前端框架，优化交互体验',
    icon: '📈'
  },
  {
    year: '2020',
    title: '你好，世界',
    description: '接触编程世界，开启 HTML/CSS/JS 之旅',
    icon: '🌱'
  }
];
---

<AetherLayout title="关于我 - Aether Lab">
  <div class="about-container">
    <!-- 个人介绍卡片 -->
    <section class="hero-card glass-card">
      <div class="hero-content">
        <div class="avatar-section">
          <div class="avatar-frame">
            <div class="avatar">👨‍💻</div>
            <div class="avatar-scan-line"></div>
          </div>
          <div class="status-indicator">
            <span class="status-dot"></span>
            <span class="status-text">状态: 编码中</span>
          </div>
        </div>
        
        <div class="intro-section">
          <h1>身份验证: <span class="highlight">开发者</span></h1>
          <p class="intro-text">
            &gt; 热爱技术的开发，专注于创造优雅的用户体验。<br/>
            &gt; 喜欢探索新技术，分享学习心得，与技术社区一起成长。
          </p>
          
          <div class="contact-links">
            <a href="mailto:3179623450@qq.com" class="contact-link">
              [邮件]
            </a>
            <a href="https://github.com/rczmm" class="contact-link">
              [代码库]
            </a>
          </div>
        </div>
      </div>
    </section>

    <!-- 统计信息 -->
    <section class="stats-section glass-card">
      <div class="section-title">系统统计</div>
      <div class="stats-grid">
        <div class="stat-item">
          <div class="stat-value">{totalPosts}</div>
          <div class="stat-label">日志数量</div>
        </div>
        <div class="stat-item">
          <div class="stat-value">{totalTags}</div>
          <div class="stat-label">TAGS_ACTIVE</div>
        </div>
        <div class="stat-item">
          <div class="stat-value">{daysDiff}</div>
          <div class="stat-label">DAYS_ONLINE</div>
        </div>
      </div>
    </section>

    <!-- 技能条 -->
    <section class="skills-section glass-card">
      <div class="section-title">技能矩阵</div>
      <div class="skills-grid">
        {skills.map(skill => (
          <div class="skill-item">
            <div class="skill-info">
              <span class="skill-name">{skill.name}</span>
              <span class="skill-level">{skill.level}%</span>
            </div>
            <div class="skill-bar-bg">
              <div class="skill-bar-fill" style={`width: ${skill.level}%`}></div>
            </div>
          </div>
        ))}
      </div>
    </section>

    <!-- 时间线 -->
    <section class="timeline-section glass-card">
      <div class="section-title">时间线</div>
      <div class="timeline">
        {timeline.map((item, index) => (
          <div class="timeline-item">
            <div class="timeline-marker"></div>
            <div class="timeline-content">
              <div class="timeline-year">{item.year}</div>
              <h3 class="timeline-title">{item.title}</h3>
              <p class="timeline-desc">{item.description}</p>
            </div>
          </div>
        ))}
      </div>
    </section>
  </div>
</AetherLayout>

<style>
  .about-container {
    max-width: 900px;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  /* 个人介绍 */
  .hero-card {
    padding: 3rem;
  }

  .hero-content {
    display: flex;
    align-items: center;
    gap: 3rem;
  }

  .avatar-frame {
    width: 150px;
    height: 150px;
    border-radius: 50%;
    border: 2px solid var(--color-primary-start);
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(6, 182, 212, 0.1);
    overflow: hidden;
  }

  .avatar {
    font-size: 5rem;
  }

  .avatar-scan-line {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 2px;
    background: var(--color-primary-light);
    box-shadow: 0 0 10px var(--color-primary-light);
    animation: scan 2s linear infinite;
  }

  .status-indicator {
    margin-top: 1rem;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    font-family: var(--font-mono);
    font-size: 0.8rem;
    color: var(--color-primary-start);
  }

  .status-dot {
    width: 8px;
    height: 8px;
    background: var(--color-primary-start);
    border-radius: 50%;
    box-shadow: 0 0 5px var(--color-primary-start);
    animation: blink 1s step-end infinite;
  }

  .intro-section h1 {
    font-size: 2rem;
    margin: 0 0 1rem;
    font-family: var(--font-mono);
    color: var(--color-text-title);
  }

  .highlight {
    color: var(--color-primary-start);
  }

  .intro-text {
    font-family: var(--font-mono);
    color: var(--color-text-body);
    line-height: 1.8;
    margin-bottom: 2rem;
  }

  .contact-links {
    display: flex;
    gap: 1.5rem;
  }

  .contact-link {
    font-family: var(--font-mono);
    color: var(--color-text-code);
    text-decoration: none;
    transition: all 0.2s;
  }

  .contact-link:hover {
    color: var(--color-primary-start);
    text-shadow: 0 0 5px rgba(6, 182, 212, 0.5);
  }

  /* 统计 */
  .section-title {
    font-family: var(--font-mono);
    font-size: 1.2rem;
    color: var(--color-neutral);
    margin-bottom: 1.5rem;
    letter-spacing: 0.1em;
    border-bottom: 1px dashed var(--color-neutral-light);
    padding-bottom: 0.5rem;
  }

  .stats-section, .skills-section, .timeline-section {
    padding: 2rem;
  }

  .stats-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
    text-align: center;
  }

  .stat-value {
    font-size: 2.5rem;
    font-weight: 700;
    color: var(--color-primary-start);
    font-family: var(--font-mono);
  }

  .stat-label {
    font-size: 0.9rem;
    color: var(--color-neutral);
    margin-top: 0.5rem;
    font-family: var(--font-mono);
  }

  /* 技能 */
  .skills-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1.5rem;
  }

  .skill-item {
    padding: 0.5rem;
  }

  .skill-info {
    display: flex;
    justify-content: space-between;
    margin-bottom: 0.5rem;
    font-family: var(--font-mono);
    font-size: 0.9rem;
  }

  .skill-bar-bg {
    height: 6px;
    background: rgba(0, 0, 0, 0.05);
    border-radius: 3px;
    overflow: hidden;
  }

  .skill-bar-fill {
    height: 100%;
    background: linear-gradient(90deg, var(--color-primary-start), var(--color-primary-end));
    border-radius: 3px;
  }

  /* 时间线 */
  .timeline {
    position: relative;
    padding-left: 1rem;
  }

  .timeline::before {
    content: '';
    position: absolute;
    left: 0;
    top: 0;
    bottom: 0;
    width: 2px;
    background: var(--color-neutral-light);
  }

  .timeline-item {
    position: relative;
    padding-left: 2rem;
    margin-bottom: 2rem;
  }

  .timeline-marker {
    position: absolute;
    left: -5px;
    top: 6px;
    width: 12px;
    height: 12px;
    background: var(--color-bg-start);
    border: 2px solid var(--color-primary-start);
    border-radius: 50%;
  }

  .timeline-year {
    font-family: var(--font-mono);
    color: var(--color-primary-start);
    font-size: 0.9rem;
    margin-bottom: 0.2rem;
  }

  .timeline-title {
    margin: 0 0 0.5rem;
    font-size: 1.1rem;
    color: var(--color-text-title);
  }

  .timeline-desc {
    margin: 0;
    color: var(--color-text-body);
    font-size: 0.95rem;
  }

  @keyframes scan {
    0% { top: 0; opacity: 0; }
    50% { opacity: 1; }
    100% { top: 100%; opacity: 0; }
  }

  @keyframes blink {
    50% { opacity: 0; }
  }

  @media (max-width: 768px) {
    .hero-content {
      flex-direction: column;
      text-align: center;
    }
    
    .skills-grid {
      grid-template-columns: 1fr;
    }

    .contact-links {
      justify-content: center;
    }
  }
</style>
