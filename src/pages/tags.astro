---
import AetherLayout from '../layouts/AetherLayout.astro';

// 获取所有博客文章
const allPosts = await Astro.glob('./posts/*.md');

// 统计标签
const tagCounts = allPosts.reduce((acc, post) => {
  const tags = post.frontmatter.tags || [];
  tags.forEach((tag: string) => {
    acc[tag] = (acc[tag] || 0) + 1;
  });
  return acc;
}, {} as Record<string, number>);

// 按文章数量排序标签
const sortedTags = Object.entries(tagCounts).sort((a, b) => b[1] - a[1]);

function formatDate(date: string) {
  return new Date(date).toLocaleDateString('zh-CN', {
    year: 'numeric',
    month: '2-digit',
    day: '2-digit'
  }).replace(/\//g, '-');
}
---

<AetherLayout title="标签 - Aether Lab">
  <div class="tags-container">
    <!-- 页面头部 -->
    <header class="tags-header glass-card">
      <h1>TAG_INDEX</h1>
      <div class="tags-stats">
        <span class="stat-item">
          <span class="label">TAGS:</span>
          <span class="value">{sortedTags.length}</span>
        </span>
        <span class="divider">|</span>
        <span class="stat-item">
          <span class="label">LOGS:</span>
          <span class="value">{allPosts.length}</span>
        </span>
      </div>
    </header>

    <!-- 标签云 -->
    <section class="tag-cloud-section glass-card">
      <div class="section-title">CLOUD_VISUALIZATION</div>
      <div class="tag-cloud">
        {sortedTags.map(([tag, count], index) => (
          <a 
            href={`#tag-${encodeURIComponent(tag)}`} 
            class="tag-cloud-item"
            style={`
              font-size: ${Math.max(0.8, Math.min(1.5, 0.8 + count * 0.1))}rem;
              opacity: ${Math.min(1, 0.5 + count * 0.1)};
            `}
          >
            <span class="bracket">[</span>
            {tag}
            <span class="tag-count">:{count}</span>
            <span class="bracket">]</span>
          </a>
        ))}
      </div>
    </section>

    <!-- 标签详情列表 -->
    <section class="tags-detail">
      {sortedTags.map(([tag, count], index) => {
        const tagPosts = allPosts.filter(post => 
          post.frontmatter.tags?.includes(tag)
        ).sort((a, b) => 
          new Date(b.frontmatter.pubDate).valueOf() - new Date(a.frontmatter.pubDate).valueOf()
        );
        
        return (
          <div class="tag-section glass-card" id={`tag-${encodeURIComponent(tag)}`}>
            <div class="tag-header">
              <h3 class="tag-title">
                <span class="hash">#</span>{tag}
              </h3>
              <span class="tag-count-badge">COUNT: {count}</span>
            </div>
            
            <div class="tag-posts">
              {tagPosts.map((post) => (
                <article class="tag-post-item">
                  <a href={post.url} class="item-link-overlay"></a>
                  <div class="post-status"></div>
                  <div class="post-info">
                    <div class="post-meta-top">
                      <span class="post-id">LOG_{post.url?.split('/').pop()?.slice(0,4).toUpperCase()}</span>
                      <time class="post-date">{formatDate(post.frontmatter.pubDate)}</time>
                    </div>
                    <h4 class="post-title">
                      {post.frontmatter.title}
                    </h4>
                    <p class="post-description">
                      {post.frontmatter.description}
                    </p>
                  </div>
                  <div class="post-arrow">&gt;</div>
                </article>
              ))}
            </div>
          </div>
        );
      })}
    </section>
  </div>
</AetherLayout>

<style>
  .tags-container {
    max-width: 900px;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    gap: 3rem;
  }

  /* 头部 */
  .tags-header {
    padding: 2rem;
    text-align: center;
  }

  .tags-header h1 {
    font-family: var(--font-mono);
    color: var(--color-text-title);
    font-size: 2rem;
    margin-bottom: 1rem;
    letter-spacing: 0.1em;
  }

  .tags-stats {
    font-family: var(--font-mono);
    font-size: 0.9rem;
    color: var(--color-neutral);
    display: flex;
    justify-content: center;
    gap: 1rem;
  }

  .stat-item .value {
    color: var(--color-primary-start);
    font-weight: bold;
    margin-left: 0.5rem;
  }

  .divider {
    color: var(--color-neutral-light);
  }

  /* 标签云 */
  .tag-cloud-section {
    padding: 2rem;
  }

  .section-title {
    font-family: var(--font-mono);
    font-size: 0.9rem;
    color: var(--color-neutral);
    margin-bottom: 1.5rem;
    letter-spacing: 0.1em;
    border-bottom: 1px dashed var(--color-neutral-light);
    padding-bottom: 0.5rem;
  }

  .tag-cloud {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    justify-content: center;
  }

  .tag-cloud-item {
    font-family: var(--font-mono);
    text-decoration: none;
    color: var(--color-text-body);
    transition: all 0.3s;
    padding: 0.2rem 0.5rem;
    border-radius: 4px;
  }

  .tag-cloud-item:hover {
    background: rgba(6, 182, 212, 0.1);
    color: var(--color-primary-start);
    transform: scale(1.1);
  }

  .bracket {
    color: var(--color-neutral-light);
  }

  .tag-count {
    font-size: 0.7em;
    color: var(--color-neutral);
  }

  /* 标签详情 */
  .tags-detail {
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  .tag-section {
    padding: 2rem;
  }

  .tag-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 1.5rem;
    border-bottom: 2px solid var(--color-primary-start);
    padding-bottom: 0.5rem;
  }

  .tag-title {
    font-family: var(--font-mono);
    font-size: 1.5rem;
    margin: 0;
    color: var(--color-text-title);
  }

  .hash {
    color: var(--color-primary-start);
    margin-right: 0.2rem;
  }

  .tag-count-badge {
    font-family: var(--font-mono);
    font-size: 0.8rem;
    color: var(--color-neutral);
    background: rgba(0,0,0,0.05);
    padding: 0.2rem 0.6rem;
    border-radius: 4px;
  }

  .tag-posts {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .tag-post-item {
    display: flex;
    align-items: center;
    gap: 1.5rem;
    padding: 1rem;
    border: 1px solid var(--color-neutral-light);
    border-radius: 8px;
    transition: all 0.3s;
    background: rgba(255, 255, 255, 0.3);
    position: relative;
  }

  .item-link-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 10;
  }

  .tag-post-item:hover {
    border-color: var(--color-primary-start);
    background: rgba(255, 255, 255, 0.6);
    transform: translateX(5px);
  }

  .post-status {
    width: 8px;
    height: 8px;
    background: var(--color-neutral-light);
    border-radius: 50%;
    transition: background 0.3s;
  }

  .tag-post-item:hover .post-status {
    background: var(--color-primary-start);
    box-shadow: 0 0 5px var(--color-primary-start);
  }

  .post-info {
    flex: 1;
  }

  .post-meta-top {
    display: flex;
    gap: 1rem;
    font-size: 0.75rem;
    font-family: var(--font-mono);
    color: var(--color-neutral);
    margin-bottom: 0.3rem;
  }

  .post-id {
    color: var(--color-primary-start);
  }

  .post-title {
    margin: 0 0 0.5rem;
    font-size: 1.1rem;
    color: var(--color-text-title);
    font-family: var(--font-sans);
  }

  .post-description {
    margin: 0;
    font-size: 0.9rem;
    color: var(--color-text-body);
    line-height: 1.5;
  }

  .post-arrow {
    font-family: var(--font-mono);
    color: var(--color-neutral-light);
    font-weight: bold;
    transition: all 0.3s;
  }

  .tag-post-item:hover .post-arrow {
    color: var(--color-primary-start);
    transform: translateX(5px);
  }

  @media (max-width: 768px) {
    .tag-header {
      flex-direction: column;
      align-items: flex-start;
      gap: 0.5rem;
    }

    .tag-post-item {
      flex-direction: column;
      align-items: flex-start;
      gap: 0.5rem;
    }

    .post-status, .post-arrow {
      display: none;
    }
  }
</style>
                        </time>
                        {post.frontmatter.author && (
                          <span class="post-author">
                            👤 {post.frontmatter.author}
                          </span>
                        )}
                      </div>
                    </div>
                  </article>
                ))}
              </div>
            </div>
          );
        })}
      </div>
    </section>

    {sortedTags.length === 0 && (
      <div class="empty-state">
        <div class="empty-icon">🏷️</div>
        <h2>暂无标签</h2>
        <p>还没有任何标签，开始创建文章并添加标签吧！</p>
      </div>
    )}
  </div>
</BaseLayout>

<style>
  .tags-container {
    max-width: 900px;
    margin: 0 auto;
  }

  .tags-header {
    text-align: center;
    margin-bottom: 3rem;
    padding: 2rem;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    border-radius: 12px;
    color: white;
  }

  .tags-header h1 {
    margin: 0 0 1rem 0;
    font-size: 2.5rem;
    font-weight: 700;
  }

  .tags-stats {
    margin: 0;
    font-size: 1.1rem;
    opacity: 0.9;
  }

  .tags-stats strong {
    font-weight: 600;
  }

  .tag-cloud-section {
    background: white;
    padding: 2rem;
    border-radius: 12px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.07);
    margin-bottom: 3rem;
    border: 1px solid #e2e8f0;
  }

  .tag-cloud-section h2 {
    margin: 0 0 1.5rem 0;
    color: #2d3748;
    font-size: 1.5rem;
  }

  .tag-cloud {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    align-items: center;
    justify-content: center;
    min-height: 200px;
  }

  .tag-cloud-item {
    text-decoration: none;
    font-weight: 600;
    transition: all 0.3s ease;
    display: inline-flex;
    align-items: baseline;
    gap: 0.25rem;
    padding: 0.5rem;
    border-radius: 8px;
  }

  .tag-cloud-item:hover {
    transform: scale(1.1);
    background: rgba(0, 0, 0, 0.05);
  }

  .tag-count {
    font-size: 0.8rem;
    opacity: 0.7;
  }

  .tags-detail h2 {
    margin: 0 0 2rem 0;
    color: #2d3748;
    font-size: 1.8rem;
  }

  .tag-section {
    background: white;
    border-radius: 12px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.07);
    margin-bottom: 2rem;
    overflow: hidden;
    border: 1px solid #e2e8f0;
  }

  .tag-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1.5rem;
    background: #f7fafc;
    border-bottom: 1px solid #e2e8f0;
  }

  .tag-title {
    margin: 0;
    font-size: 1.4rem;
    font-weight: 600;
  }

  .tag-count-badge {
    background: #edf2f7;
    color: #4a5568;
    padding: 0.25rem 0.75rem;
    border-radius: 20px;
    font-size: 0.9rem;
    font-weight: 500;
  }

  .tag-posts {
    padding: 1rem;
  }

  .tag-post-item {
    border-bottom: 1px solid #e2e8f0;
    padding: 1rem 0;
    transition: all 0.2s ease;
  }

  .tag-post-item:last-child {
    border-bottom: none;
  }

  .tag-post-item:hover {
    background: #f7fafc;
    margin: 0 -1rem;
    padding: 1rem;
    border-radius: 8px;
  }

  .post-title {
    margin: 0 0 0.5rem 0;
    font-size: 1.2rem;
    line-height: 1.3;
  }

  .post-title a {
    color: #2d3748;
    text-decoration: none;
    transition: color 0.2s ease;
  }

  .post-title a:hover {
    color: #3182ce;
  }

  .post-description {
    color: #718096;
    line-height: 1.6;
    margin: 0 0 0.75rem 0;
    font-size: 0.95rem;
  }

  .post-meta {
    display: flex;
    gap: 1rem;
    font-size: 0.875rem;
    color: #a0aec0;
    flex-wrap: wrap;
  }

  .empty-state {
    text-align: center;
    padding: 4rem 2rem;
    color: #718096;
  }

  .empty-icon {
    font-size: 4rem;
    margin-bottom: 1rem;
  }

  .empty-state h2 {
    margin: 0 0 1rem 0;
    color: #2d3748;
  }

  .empty-state p {
    margin: 0;
    font-size: 1.1rem;
  }

  @media (max-width: 768px) {
    .tags-header {
      padding: 1.5rem;
      margin-left: -1rem;
      margin-right: -1rem;
      border-radius: 0;
    }

    .tags-header h1 {
      font-size: 2rem;
    }

    .tag-cloud-section {
      padding: 1.5rem;
    }

    .tag-cloud {
      min-height: 150px;
    }

    .tag-header {
      flex-direction: column;
      align-items: flex-start;
      gap: 1rem;
    }

    .post-meta {
      flex-direction: column;
      gap: 0.5rem;
    }
  }
</style>