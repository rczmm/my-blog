---
import BaseLayout from '../layouts/BaseLayout.astro';
import BlogCarousel from '../components/BlogCarousel.astro';
import ActivityCalendar from '../components/ActivityCalendar.astro';

// 获取所有博客文章
const allPosts = await Astro.glob('./posts/*.md');

// 按发布日期降序排序
allPosts.sort((a, b) => new Date(b.frontmatter.pubDate).valueOf() - new Date(a.frontmatter.pubDate).valueOf());

// 格式化日期的函数
function formatDate(date: string) {
  return new Date(date).toLocaleDateString('zh-CN', {
    year: 'numeric',
    month: 'long',
    day: 'numeric'
  });
}
---

<BaseLayout title="我的博客">
  <!-- 博客轮播图 -->
  <BlogCarousel posts={allPosts} />
  
  <!-- 统计信息卡片 -->
  <section class="stats-section">
    <div class="stats-grid">
      <div class="stat-card">
        <div class="stat-icon">📝</div>
        <div class="stat-content">
          <h3>{allPosts.length}</h3>
          <p>篇文章</p>
        </div>
      </div>
      
      <div class="stat-card">
        <div class="stat-icon">🏷️</div>
        <div class="stat-content">
          <h3>{new Set(allPosts.flatMap(post => post.frontmatter.tags || [])).size}</h3>
          <p>个标签</p>
        </div>
      </div>
      
      <div class="stat-card">
        <div class="stat-icon">👀</div>
        <div class="stat-content">
          <h3>1.2K</h3>
          <p>总访问</p>
        </div>
      </div>
      
      <div class="stat-card">
        <div class="stat-icon">📅</div>
        <div class="stat-content">
          <h3>{Math.ceil((Date.now() - new Date('2023-01-01').getTime()) / (1000 * 60 * 60 * 24))}</h3>
          <p>天运行</p>
        </div>
      </div>
    </div>
  </section>

  <!-- 活动热力图 -->
  <ActivityCalendar posts={allPosts} />

  <!-- 最新文章区域 -->
  <section class="latest-posts">
    <div class="section-header">
      <h2>📚 最新文章</h2>
      <a href="/archive" class="more-link">查看全部 →</a>
    </div>
    <div class="posts-grid">
      {allPosts.length === 0 ? (
        <div class="no-posts">
          <p>暂无文章，敬请期待...</p>
        </div>
      ) : (
        allPosts.slice(0, 6).map((post) => (
          <article class="post-card">
            <div class="post-card-content">
              <h3 class="post-title">
                <a href={post.url}>{post.frontmatter.title}</a>
              </h3>
              
              <p class="post-description">
                {post.frontmatter.description}
              </p>
              
              <div class="post-meta">
                <time class="post-date">
                  📅 {formatDate(post.frontmatter.pubDate)}
                </time>
                
                {post.frontmatter.author && (
                  <span class="post-author">
                    👤 {post.frontmatter.author}
                  </span>
                )}
              </div>
              
              {post.frontmatter.tags && post.frontmatter.tags.length > 0 && (
                <div class="post-tags">
                  {post.frontmatter.tags.slice(0, 3).map((tag: string) => (
                    <span class="tag">{tag}</span>
                  ))}
                </div>
              )}
              
              <div class="post-footer">
                <a href={post.url} class="read-more">
                  阅读全文 →
                </a>
              </div>
            </div>
          </article>
        ))
      )}
    </div>
  </section>
</BaseLayout>

<style>
  /* 统计卡片样式 */
  .stats-section {
    margin: 2rem 0;
  }

  .stats-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 1.5rem;
  }

  .stat-card {
    background: white;
    padding: 1.5rem;
    border-radius: 12px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.07);
    display: flex;
    align-items: center;
    gap: 1rem;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    border: 1px solid #e2e8f0;
  }

  .stat-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 12px 24px rgba(0, 0, 0, 0.15);
  }

  .stat-icon {
    font-size: 2.5rem;
    opacity: 0.8;
  }

  .stat-content h3 {
    margin: 0;
    font-size: 2rem;
    font-weight: 700;
    color: #2d3748;
  }

  .stat-content p {
    margin: 0;
    color: #718096;
    font-size: 0.9rem;
  }

  /* 区域头部 */
  .section-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 2rem;
  }

  .section-header h2 {
    margin: 0;
    color: #2d3748;
    font-size: 1.8rem;
  }

  .more-link {
    color: #3182ce;
    font-weight: 600;
    text-decoration: none;
    transition: color 0.2s ease;
  }

  .more-link:hover {
    color: #2c5aa0;
  }

  /* 文章网格样式 */
  .latest-posts {
    margin: 3rem 0;
  }

  .posts-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
    gap: 2rem;
  }

  .post-card {
    background: white;
    border-radius: 12px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.07);
    transition: all 0.3s ease;
    overflow: hidden;
    border: 1px solid #e2e8f0;
  }

  .post-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 12px 24px rgba(0, 0, 0, 0.15);
  }

  .post-card-content {
    padding: 1.5rem;
  }

  .post-title {
    margin: 0 0 1rem 0;
    font-size: 1.3rem;
    line-height: 1.3;
  }

  .post-title a {
    color: #2d3748;
    text-decoration: none;
    font-weight: 600;
    transition: color 0.2s ease;
  }

  .post-title a:hover {
    color: #3182ce;
  }

  .post-description {
    color: #718096;
    line-height: 1.6;
    margin: 0 0 1.5rem 0;
    font-size: 0.95rem;
  }

  .post-meta {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    margin-bottom: 1rem;
    font-size: 0.875rem;
    color: #a0aec0;
  }

  .post-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin-bottom: 1.5rem;
  }

  .tag {
    background: #edf2f7;
    color: #4a5568;
    padding: 0.25rem 0.75rem;
    border-radius: 20px;
    font-size: 0.8rem;
    font-weight: 500;
    transition: all 0.2s ease;
  }

  .tag:hover {
    background: #e2e8f0;
    transform: scale(1.05);
  }

  .post-footer {
    border-top: 1px solid #e2e8f0;
    padding-top: 1rem;
    margin-top: 1rem;
  }

  .read-more {
    color: #3182ce;
    font-weight: 600;
    text-decoration: none;
    font-size: 0.9rem;
    transition: all 0.2s ease;
    display: inline-flex;
    align-items: center;
    gap: 0.25rem;
  }

  .read-more:hover {
    color: #2c5aa0;
    transform: translateX(2px);
  }

  .no-posts {
    grid-column: 1 / -1;
    text-align: center;
    padding: 3rem;
    color: #a0aec0;
    font-size: 1.1rem;
    background: white;
    border-radius: 12px;
    border: 2px dashed #e2e8f0;
  }

  /* 响应式设计 */
  @media (max-width: 768px) {
    .stats-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 1rem;
    }

    .stat-card {
      padding: 1rem;
    }

    .stat-icon {
      font-size: 2rem;
    }

    .stat-content h3 {
      font-size: 1.5rem;
    }

    .section-header {
      flex-direction: column;
      align-items: flex-start;
      gap: 1rem;
    }

    .posts-grid {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }

    .post-card-content {
      padding: 1.25rem;
    }
  }

  @media (max-width: 480px) {
    .stats-grid {
      grid-template-columns: 1fr;
    }
  }
</style>